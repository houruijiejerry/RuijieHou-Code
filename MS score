# MS score (AddModule Score Method)
geneset=list(c('gene1','gene2',....)
scRNA<-AddModuleScore(scRNA,
                      features=geneset,
                      name="MS_AddModuleScore")
MS_table=FetchData(scRNA,vars=c('group','MS_AddModuleScore1'))
